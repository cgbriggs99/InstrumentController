#ifndef __WIFI_SECRETS__
#define __WIFI_SECRETS__

// Wifi information
static const char *ssid = ; // Wifi name
static const char *passwd = ; // Wifi password
static const char *server = ; // IP address of MQTT broker
static uint16_t port = 1883; // Port of MQTT broker. Default 1883

#endif 
